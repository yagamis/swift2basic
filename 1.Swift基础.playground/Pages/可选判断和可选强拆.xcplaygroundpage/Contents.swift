//: 可选判断: 使用if语句来判断可选变量是否有值(与nil比较).
//: - 🌰用户性别是否选择
var sex: Bool? = false

if sex != nil {
   print("有性别")
} else {
   print("未选性别")
}

//: 可选强拆 : 把可选变量中的值强制解压出来.语法是紧跟变量名后加感叹号，⚠️谨慎使用
sex!


//: [可选绑定](@next)
