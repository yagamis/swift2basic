//: 可选判断: 使用if语句来判断可选变量是否有值(与nil比较).
//: - 🌰字符串123的整型转换是否有值
let 身份证号1 = "2102199502132560"
var 纯数字的身份证号 = Int(身份证号1)

if 纯数字的身份证号 != nil {
   print("成功转换了!是数字的身份证")
} else {
  print("不能转换")
    
}

//: 可选强拆 : 把可选变量中的值强制解压出来.语法是紧跟变量名后加感叹号!
//: - 🌰对上例的变量进行强拆
纯数字的身份证号!

//: - ⚠️如果无值强拆,会遇到运行时错误.会导致app崩溃.
//: - 🌰对字符串123a的整型转换结果,强拆
let 身份证号2 = "421619930809131X"
纯数字的身份证号 = Int(身份证号2)


//: [可选绑定](@next)
